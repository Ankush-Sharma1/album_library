<!DOCTYPE html>
<html>
<head>
	<title>Home Page</title>
</head>
<body>
	<% if user_signed_in? %>
		<%= link_to('Logout', destroy_user_session_path, :method => :delete, :class => "button btn btn-danger" )%>
	<% else %>
		<%= link_to('Login', new_user_session_path, :class => "button btn btn-success" )%>
	<% end %>
</body>
</html>